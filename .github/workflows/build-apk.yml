      - name: Install Buildozer
        run: |
          python -m venv ~/.venv
          source ~/.venv/bin/activate
          pip install --upgrade pip setuptools wheel
          # Use a valid reference (master branch) instead of a non‑existent SHA
          pip install git+https://github.com/kivy/buildozer.git@master
          echo "~/.venv/bin" >> $GITHUB_PATH

      - name: Install Android SDK & NDK
        env:
          ANDROID_SDK_ROOT: ${{ runner.temp }}/android-sdk
          ANDROID_NDK_ROOT: ${{ runner.temp }}/android-ndk
        run: |
          mkdir -p "$ANDROID_SDK_ROOT/cmdline-tools"
          cd "$ANDROID_SDK_ROOT/cmdline-tools"
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O tools.zip
          unzip -q tools.zip
          mv cmdline-tools latest
          export PATH="$ANDROID_SDK_ROOT/cmdline-tools/latest/bin:$PATH"
          yes | sdkmanager --sdk_root="$ANDROID_SDK_ROOT" "platform-tools" "platforms;android-36" "build-tools;36.0.0"
          # Install exact NDK version that exists in the repository
          yes | sdkmanager --sdk_root="$ANDROID_SDK_ROOT" "ndk;27.0.11902837"
          yes | sdkmanager --sdk_root="$ANDROID_SDK_ROOT" --licenses
          echo "$ANDROID_SDK_ROOT" >> $GITHUB_PATH
          echo "$ANDROID_NDK_ROOT" >> $GITHUB_PATH
