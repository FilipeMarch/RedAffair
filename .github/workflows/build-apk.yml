name: Build A Poop Knife

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Make Poop Knife Blank
        run: |
          pip install --upgrade pip
          pip install git+https://github.com/kivy-school/ksproject.git

      - name: Make Poop Knife Sharp
        run: |
          ksproject init --name "Red Affair"

      - name: Build Poop Knife Handle and Attach
        run: |
          ksproject build --target android --release

      - name: Assemble A Poop Knife
        uses: actions/upload-artifact@v4
        with:
          name: redaffair-apk
          path: dist/*.apk
          if-no-files-found: error